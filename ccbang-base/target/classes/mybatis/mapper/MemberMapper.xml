<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ccbang.base.dao.MemberDAO">

	<!-- <sql id="all_column">
		id,name,description,img_src,
		version,create_by,create_dt,update_by,update_dt 
	</sql> -->
            
         <!-- <insert id="insertUser" parameterType="User" keyProperty="id">
             insert into days_user( 
         state,
         nickname)
         values
         (       
         #{state},
         #{nickname})
         </insert> -->
         
	<!-- <insert id="insert" parameterType="Address" keyProperty="addrId">
		<selectKey ></selectKey>
		INSERT INTO ADDRESSES 
		(STREET,CITY,STATE,ZIP,COUNTRY) VALUES 
 		(#{street},#{city},#{state},#{zip},#{country})
	</insert> -->
	
	<!-- <select id="getById" resultMap="AddressResult">
		select distinct 
		<include refid="all_column" /> 
		from tbl_member 
		<if test="whereClause != null">
			where
		</if>
	</select> -->
	
	<!-- <select id="queryByPage" resultMap="base_member_result">
		select * from ADDRESSES where addr_id = #{addrId}
	</select> -->
	
	

	
	
	<!-- <resultMap type="Member" id="base_member_result">
  		<id property="id" column="id" jdbcType="CHAR"/>
		<result property="name" column="name" jdbcType="VARCHAR"/>
		<result property="description" column="description" jdbcType="VARCHAR"/>
		<result property="imgSrc" column="img_src" jdbcType="VARCHAR"/>
		<result column="version" property="version" jdbcType="INTEGER" />
	    <result column="created_by" property="createdBy" jdbcType="VARCHAR" />
	    <result column="created_dt" property="createdDt" jdbcType="TIMESTAMP" />
	    <result column="updated_by" property="updatedBy" jdbcType="VARCHAR" />
	    <result column="updated_dt" property="updatedDt" jdbcType="TIMESTAMP" />
  	</resultMap> -->
          
</mapper>
